{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.5.1",
    "rxjs": "^6.3.3",
    "rxjs-tslint-rules": "^4.13.0",
    "tslint": "^5.11.0",
    "typescript": "^3.2.2"
  }
}
